/// @description 

if(!audio_group_is_loaded(audiogroup_default))audio_group_load(audiogroup_default)
if (room_music[room] != undefined) { //make sure that their is actually a music track set for the room
	var musicNext = room_music[room];
	next_track = musicNext // if this all checks out, set the new track		
}	